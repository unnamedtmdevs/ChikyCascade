# Инструкция по интеграции AppsFlyer и WebView

## ✅ Что уже сделано

1. ✅ Создан `AppsFlyerManager` - менеджер для работы с AppsFlyer SDK
2. ✅ Создан `CampaignWebView` - мощный WebView с настройками для обхода детекции
3. ✅ Создан `AttributionViewModel` - управление логикой показа WebView
4. ✅ Создан `AppConfig` - конфигурация с ключами:
   - AppsFlyer Dev Key: `PcQdjvdLQJPZhHXH5PDtg3`
   - Apple App ID: `6755767477`
   - Campaign URL: `https://chikycascade102.site/44yfyzmW`
5. ✅ Интегрировано в `CluckCascadeApp` и `AppRootView`

## 📦 Что нужно сделать в Xcode

### 1. Добавить AppsFlyer SDK через Swift Package Manager

1. Откройте проект в Xcode
2. В меню выберите **File → Add Package Dependencies...**
3. Вставьте URL репозитория:
   ```
   https://github.com/AppsFlyerSDK/AppsFlyerFramework
   ```
4. Нажмите **Add Package**
5. Выберите версию (рекомендуется последняя стабильная, например `6.17.8`)
6. Нажмите **Add Package**
7. Убедитесь, что пакет добавлен к таргету **ChikyCascade**

### 2. Проверка интеграции

После добавления SDK:
1. Убедитесь, что проект компилируется без ошибок
2. Запустите приложение
3. Проверьте логи в консоли - должны появиться сообщения от AppsFlyer

## 🔧 Настройка тестирования

В файле `CluckCascade/Config/TestConfig.swift`:

```swift
struct TestConfig {
    // Установите true чтобы принудительно показать WebView (для тестирования)
    static let forceShowWebView = false
    
    // Дополнительное логирование
    static let verboseLogging = false
}
```

**Для тестирования WebView:**
- Установите `forceShowWebView = true` - это покажет WebView независимо от данных AppsFlyer

## 📱 Как это работает

1. При запуске приложения инициализируется `AppsFlyerManager`
2. AppsFlyer проверяет атрибуцию установки (органическая/неорганическая)
3. Если установка **неорганическая** (с рекламы):
   - Показывается `CampaignWebView` с URL кампании
   - URL автоматически дополняется параметром `utm_placement` с AppsFlyer ID
4. Если установка **органическая**:
   - Показывается обычный flow приложения (Onboarding → Main)

## 🎯 Особенности WebView

WebView настроен для обхода anti-bot защиты:
- Реалистичный User-Agent (iPhone Safari)
- Правильные HTTP заголовки
- Управление cookies (сохранение и загрузка)
- Настройки JavaScript
- Поддержка медиа контента

## ⚠️ Важно

- Перед релизом установите `isDebug = false` в `AppsFlyerManager.swift`
- Проверьте работу на реальных устройствах
- Убедитесь, что URL кампании корректен

## 🐛 Отладка

Если что-то не работает:
1. Проверьте логи в консоли Xcode
2. Установите `verboseLogging = true` в `TestConfig.swift`
3. Проверьте, что AppsFlyer SDK правильно добавлен к проекту
4. Убедитесь, что URL кампании доступен

